swayidle \
	timeout 290 'dim -d 10 && systemctl suspend' 
	# timeout 290 'dim -d 10 && hyprlock --no-fade-in' \
	# timeout 900 'systemctl suspend' \
	# before-sleep 'hyprlock --no-fade-in' >/dev/null 2>&1 &
